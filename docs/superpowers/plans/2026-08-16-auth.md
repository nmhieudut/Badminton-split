# Giai đoạn 2 — Đăng nhập và phân quyền — Kế hoạch triển khai

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Khóa mọi đường ghi dữ liệu sau lớp đăng nhập Google, trong khi trang vẫn xem được công khai.

**Architecture:** Supabase Auth với Google OAuth, phiên lưu trong cookie qua `@supabase/ssr`. Hai mức quyền: super admin đến từ biến môi trường `ADMIN_EMAILS`, admin lưu trong bảng `admins` do super admin quản lý. Chốt chặn nằm ở `requireAdmin()` và `requireSuperAdmin()` đặt đầu mỗi Server Action — không phải RLS, vì app kết nối bằng vai trò `postgres` vốn bỏ qua mọi policy.

**Tech Stack:** Next.js 15 App Router, `@supabase/ssr`, `@supabase/supabase-js`, TypeScript, Vitest, Bun.

**Spec:** `docs/superpowers/specs/2026-08-16-auth-design.md`

## Global Constraints

- Trình quản lý gói là **Bun**. Dùng `bun add`, `bun run`, `bunx`.
- Cookie của Supabase **chỉ được xử lý bằng `getAll` và `setAll`**. Không bao giờ dùng `get`, `set`, `remove` — bản cũ dùng chúng và sẽ làm người dùng bị đăng xuất ngẫu nhiên.
- Không import từ `@supabase/auth-helpers-nextjs`; gói đó đã bị thay thế.
- **Middleware chỉ làm mới phiên, TUYỆT ĐỐI không chuyển hướng về trang đăng nhập.** Ví dụ mẫu trong tài liệu Supabase có phần chuyển hướng đó; chép nguyên si sẽ chặn hết khách và phá vỡ yêu cầu "trang công khai".
- Giữa `createServerClient(...)` và `supabase.auth.getUser()` **không được chèn code nào**.
- `ADMIN_EMAILS` là danh sách email ngăn cách bằng **dấu phẩy**; bỏ qua chữ hoa chữ thường và khoảng trắng thừa. **Để trống thì không có super admin nào** — hỏng theo hướng an toàn.
- Email trong bảng `admins` **luôn lưu dạng chữ thường**, chuẩn hóa ngay lúc ghi.
- Hai action quản lý quyền dùng `requireSuperAdmin()`, tuyệt đối không phải `requireAdmin()`. Hạ nhầm là admin tự nhân bản quyền được.
- Lỗi cấu hình dùng `ConfigError` từ `lib/errors.ts`: chi tiết vào log server, người dùng chỉ thấy câu chung. Lỗi phân quyền cũng không nêu chi tiết cơ chế.
- Ngôn ngữ giao diện là **tiếng Việt**. Comment trong code tiếng Việt. **Thông điệp commit tiếng Anh.**
- `"strict": true`. Mỗi task kết thúc bằng một commit.

---

## Cấu trúc tệp

| Tệp | Trách nhiệm |
|---|---|
| `lib/auth/admin-emails.ts` | Hàm thuần: email này có trong `ADMIN_EMAILS` không |
| `lib/supabase/server.ts` | Supabase client phía server, đọc cookie qua `next/headers` |
| `lib/supabase/client.ts` | Supabase client phía trình duyệt |
| `lib/auth/session.ts` | `getSessionUser()`, `getVaiTro()`, `requireAdmin()`, `requireSuperAdmin()` |
| `app/actions/admins.ts` | `addAdmin()`, `removeAdmin()` — chỉ super admin |
| `components/AdminsModal.tsx` | Quản lý danh sách admin — chỉ super admin |
| `middleware.ts` | Làm mới phiên; không chặn, không chuyển hướng |
| `app/auth/callback/route.ts` | Đổi mã OAuth lấy phiên rồi quay về trang cũ |
| `app/actions/auth.ts` | `signInWithGoogle()`, `signOut()` |
| `components/AuthButton.tsx` | Nút đăng nhập, hoặc email + đăng xuất khi đã vào |
| `components/LoginModal.tsx` | Modal một nút Google |

**Sửa:** cả 4 tệp trong `app/actions/`, `components/Navbar.tsx`, `app/[monthKey]/layout.tsx`, ba trang tab, và các component có nút ghi.

---

## Task 1: Nhận diện super admin từ biến môi trường

Hàm thuần, không phụ thuộc gì — làm được ngay, và là nền cho mọi task sau.

**Files:**
- Create: `lib/auth/admin-emails.ts`, `lib/auth/admin-emails.test.ts`

**Interfaces:**
- Produces: `laEmailAdmin(email: string | null | undefined): boolean`

- [ ] **Step 1: Viết test thất bại**

`lib/auth/admin-emails.test.ts`:

```typescript
import { afterEach, describe, expect, it } from 'vitest';
import { laEmailAdmin } from './admin-emails';

const datBien = (v: string | undefined) => {
  if (v === undefined) delete process.env.ADMIN_EMAILS;
  else process.env.ADMIN_EMAILS = v;
};

afterEach(() => datBien(undefined));

describe('laEmailAdmin', () => {
  it('nhận đúng email có trong danh sách', () => {
    datBien('a@gmail.com,b@gmail.com');
    expect(laEmailAdmin('a@gmail.com')).toBe(true);
    expect(laEmailAdmin('b@gmail.com')).toBe(true);
  });

  it('từ chối email ngoài danh sách', () => {
    datBien('a@gmail.com');
    expect(laEmailAdmin('c@gmail.com')).toBe(false);
  });

  it('bỏ qua chữ hoa chữ thường', () => {
    datBien('Admin@Gmail.com');
    expect(laEmailAdmin('admin@gmail.com')).toBe(true);
    expect(laEmailAdmin('ADMIN@GMAIL.COM')).toBe(true);
  });

  it('bỏ qua khoảng trắng thừa quanh dấu phẩy', () => {
    datBien('  a@gmail.com ,  b@gmail.com  ');
    expect(laEmailAdmin('a@gmail.com')).toBe(true);
    expect(laEmailAdmin('b@gmail.com')).toBe(true);
  });

  it('biến môi trường để trống thì không ai là super admin', () => {
    datBien('');
    expect(laEmailAdmin('a@gmail.com')).toBe(false);
  });

  it('biến môi trường không khai báo thì không ai là super admin', () => {
    datBien(undefined);
    expect(laEmailAdmin('a@gmail.com')).toBe(false);
  });

  it('email rỗng hoặc null không bao giờ là super admin', () => {
    datBien('a@gmail.com,');
    expect(laEmailAdmin('')).toBe(false);
    expect(laEmailAdmin(null)).toBe(false);
    expect(laEmailAdmin(undefined)).toBe(false);
  });

  it('không khớp một phần — email gần giống vẫn bị từ chối', () => {
    datBien('admin@gmail.com');
    expect(laEmailAdmin('admin@gmail.com.vn')).toBe(false);
    expect(laEmailAdmin('xadmin@gmail.com')).toBe(false);
  });
});
```

- [ ] **Step 2: Chạy test để xác nhận nó thất bại**

Run: `bun run test lib/auth/admin-emails.test.ts`
Expected: FAIL, không tìm thấy module `./admin-emails`.

- [ ] **Step 3: Viết cài đặt**

`lib/auth/admin-emails.ts`:

```typescript
/**
 * Email này có phải SUPER ADMIN không — đọc từ biến môi trường ADMIN_EMAILS.
 *
 * Quyền cao nhất để ở biến môi trường chứ không phải database là có chủ đích:
 * chỉ đổi được bằng cách deploy lại, nên không có đường nào chiếm nó qua giao
 * diện. Admin thường thì nằm trong bảng `admins` và do super admin quản lý.
 *
 * Đọc lại biến ở mỗi lần gọi thay vì cache, để đổi biến trên Vercel là có hiệu
 * lực ngay ở lần gọi kế tiếp.
 */
export function laEmailAdmin(email: string | null | undefined): boolean {
  if (!email) return false;

  const danhSach = (process.env.ADMIN_EMAILS ?? '')
    .split(',')
    .map((e) => e.trim().toLowerCase())
    .filter(Boolean);

  // Danh sách rỗng nghĩa là không có super admin nào. Hỏng theo hướng an toàn:
  // quên khai báo biến thì mất tính năng, chứ không phải mở toang cửa.
  if (danhSach.length === 0) return false;

  return danhSach.includes(email.trim().toLowerCase());
}
```

- [ ] **Step 4: Chạy test để xác nhận nó qua**

Run: `bun run test lib/auth/admin-emails.test.ts`
Expected: PASS, 8 test.

- [ ] **Step 5: Commit**

```bash
git add lib/auth/
git commit -m "feat: match super admin emails from ADMIN_EMAILS

Fails closed: an empty or missing variable means there is no super
admin, so forgetting to configure it loses a feature rather than
opening the door.

The highest privilege lives in an environment variable rather than the
database so it can only change through a deploy — there is no path to
claim it through the UI."
```

---

## Task 2: Bảng admins, Supabase client và trạng thái phiên

**Files:**
- Create: `lib/supabase/server.ts`, `lib/supabase/client.ts`, `lib/auth/session.ts`
- Modify: `db/schema.ts`, `db/queries.ts`, `package.json`

**Interfaces:**
- Consumes: `laEmailAdmin` từ Task 1; `ConfigError` từ `lib/errors.ts`.
- Produces:
  - bảng `admins` trong `db/schema.ts`
  - `listAdmins(): Promise<{ email: string; addedAt: Date; addedBy: string | null }[]>`
  - `taoServerClient(): Promise<SupabaseClient>`
  - `taoBrowserClient(): SupabaseClient`
  - `getSessionUser(): Promise<{ email: string | null } | null>`
  - `getVaiTro(): Promise<'super_admin' | 'admin' | null>`
  - `requireAdmin(): Promise<void>` — cho qua admin hoặc super admin
  - `requireSuperAdmin(): Promise<void>` — chỉ super admin

- [ ] **Step 1: Cài gói**

```bash
bun add @supabase/ssr
```

`@supabase/supabase-js` đã có sẵn từ Giai đoạn 1.

- [ ] **Step 2: Thêm bảng admins vào schema**

Thêm vào cuối `db/schema.ts`:

```typescript
/**
 * Admin do super admin thêm vào. Super admin thì đến từ biến môi trường
 * ADMIN_EMAILS, không nằm ở đây — quyền cao nhất không được sửa qua giao diện.
 *
 * Email làm khóa chính vì đó chính là thứ Google trả về và là thứ ta so khớp.
 * Luôn lưu dạng chữ thường, chuẩn hóa ngay lúc ghi.
 */
export const admins = pgTable('admins', {
  email: text('email').primaryKey(),
  addedAt: timestamp('added_at', { withTimezone: true }).notNull().defaultNow(),
  addedBy: text('added_by'),
});
```

- [ ] **Step 3: Sinh và chạy migration**

```bash
bun run db:generate
bun run db:migrate
```

Expected: tệp SQL mới tạo bảng `admins`, migrate báo thành công.

Bật RLS cho bảng mới, đồng bộ với 8 bảng còn lại. Tạo migration tùy chỉnh:

```bash
bunx drizzle-kit generate --custom --name=enable_rls_on_admins
```

Nội dung tệp vừa sinh:

```sql
-- Đồng bộ với các bảng khác: chặn đường truy cập trực tiếp bằng anon key.
-- App đi bằng vai trò postgres nên không bị policy này ràng buộc.
ALTER TABLE "admins" ENABLE ROW LEVEL SECURITY;
```

Rồi `bun run db:migrate` lần nữa.

- [ ] **Step 4: Hàm đọc danh sách admin**

Thêm vào `db/queries.ts` (nhớ thêm `admins` vào danh sách import từ `./schema`):

```typescript
export async function listAdmins() {
  return db
    .select({
      email: admins.email,
      addedAt: admins.addedAt,
      addedBy: admins.addedBy,
    })
    .from(admins)
    .orderBy(asc(admins.email));
}
```

- [ ] **Step 5: Client phía server**

`lib/supabase/server.ts`:

```typescript
import { createServerClient } from '@supabase/ssr';
import { cookies } from 'next/headers';
import { ConfigError } from '../errors';

/**
 * Supabase client dùng trong Server Component, Server Action và Route Handler.
 *
 * CHỈ dùng getAll/setAll cho cookie. Các API get/set/remove kiểu cũ gây ra
 * chuyện người dùng bị đăng xuất ngẫu nhiên và rất khó truy nguyên.
 */
export async function taoServerClient() {
  const url = process.env.NEXT_PUBLIC_SUPABASE_URL;
  const key = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;
  if (!url || !key) {
    throw new ConfigError(
      'Thiếu NEXT_PUBLIC_SUPABASE_URL hoặc NEXT_PUBLIC_SUPABASE_ANON_KEY'
    );
  }

  const cookieStore = await cookies();

  return createServerClient(url, key, {
    cookies: {
      getAll() {
        return cookieStore.getAll();
      },
      setAll(cookiesToSet) {
        try {
          cookiesToSet.forEach(({ name, value, options }) =>
            cookieStore.set(name, value, options)
          );
        } catch {
          // Server Component không được ghi cookie. Bỏ qua an toàn vì
          // middleware đã lo việc làm mới phiên.
        }
      },
    },
  });
}
```

- [ ] **Step 6: Client phía trình duyệt**

`lib/supabase/client.ts`:

```typescript
import { createBrowserClient } from '@supabase/ssr';

/**
 * Supabase client chạy trong trình duyệt. Chỉ dùng cho luồng đăng nhập.
 *
 * Hai biến NEXT_PUBLIC_ này đi xuống client theo đúng thiết kế: anon key là
 * khóa công khai. Tuyệt đối không đưa service role key vào đây.
 */
export function taoBrowserClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  );
}
```

- [ ] **Step 7: Trạng thái phiên và chốt chặn**

`lib/auth/session.ts`:

```typescript
import { eq } from 'drizzle-orm';
import { db } from '../../db';
import { admins } from '../../db/schema';
import { taoServerClient } from '../supabase/server';
import { laEmailAdmin } from './admin-emails';

export type VaiTro = 'super_admin' | 'admin' | null;

/**
 * Người đang đăng nhập, hoặc null nếu là khách.
 *
 * Dùng getUser() chứ không phải getSession(): getUser() hỏi lại máy chủ auth
 * để xác thực token, còn getSession() chỉ đọc cookie nên có thể bị giả mạo.
 */
export async function getSessionUser(): Promise<{ email: string | null } | null> {
  const supabase = await taoServerClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();

  return user ? { email: user.email ?? null } : null;
}

/**
 * Vai trò của người đang đăng nhập.
 *
 * Biến môi trường luôn thắng: một email vừa có trong ADMIN_EMAILS vừa có trong
 * bảng admins thì tính là super admin.
 */
export async function getVaiTro(): Promise<VaiTro> {
  const user = await getSessionUser();
  const email = user?.email?.trim().toLowerCase();
  if (!email) return null;

  if (laEmailAdmin(email)) return 'super_admin';

  const [dong] = await db
    .select({ email: admins.email })
    .from(admins)
    .where(eq(admins.email, email))
    .limit(1);

  return dong ? 'admin' : null;
}

/** Thông báo chung, không nêu chi tiết cơ chế kiểm quyền. */
const KHONG_DU_QUYEN = 'Bạn không có quyền thực hiện thao tác này.';

/**
 * Chốt chặn của mọi Server Action ghi dữ liệu nghiệp vụ.
 *
 * Đây là nơi quyền được thực thi thật sự. RLS không giúp gì: app kết nối bằng
 * vai trò `postgres`, vốn có rolbypassrls và sở hữu mọi bảng, nên policy không
 * bao giờ áp lên truy vấn của chính mình.
 */
export async function requireAdmin(): Promise<void> {
  const vaiTro = await getVaiTro();
  if (vaiTro === 'admin' || vaiTro === 'super_admin') return;

  console.warn('[phân quyền] từ chối thao tác ghi: không đủ quyền');
  throw new Error(KHONG_DU_QUYEN);
}

/**
 * Chốt chặn riêng cho việc quản lý danh sách admin.
 *
 * Phải là hàm tách biệt, không phải một tham số của requireAdmin(): tách ra thì
 * dùng nhầm là lỗi nhìn thấy được, còn gộp lại thì quên truyền cờ sẽ âm thầm hạ
 * quyền xuống mức thấp hơn. Admin tự thêm được admin khác là ranh giới giữa hai
 * nhóm biến mất mà không có gì báo.
 */
export async function requireSuperAdmin(): Promise<void> {
  if ((await getVaiTro()) === 'super_admin') return;

  console.warn('[phân quyền] từ chối thao tác quản lý admin: không phải super admin');
  throw new Error(KHONG_DU_QUYEN);
}
```

- [ ] **Step 8: Kiểm tra biên dịch**

Run: `bun run lint`
Expected: không lỗi kiểu.

- [ ] **Step 9: Commit**

```bash
git add lib/supabase/ lib/auth/session.ts db/ package.json bun.lock
git commit -m "feat: Supabase server and browser clients, session helpers

Uses getUser() rather than getSession() so the token is verified
against the auth server instead of trusted from a cookie.

Cookie handling is getAll/setAll only; the older get/set/remove API
causes users to be logged out at random."
```

---

## Task 3: Luồng đăng nhập Google

**Files:**
- Create: `middleware.ts`, `app/auth/callback/route.ts`, `app/actions/auth.ts`

**Interfaces:**
- Consumes: `taoServerClient` từ Task 2.
- Produces: `signInWithGoogle(next?: string)`, `signOut()`.

- [ ] **Step 1: Middleware làm mới phiên**

`middleware.ts` ở thư mục gốc:

```typescript
import { createServerClient } from '@supabase/ssr';
import { NextResponse, type NextRequest } from 'next/server';

/**
 * Chỉ làm mới phiên, KHÔNG chặn ai cả.
 *
 * Ví dụ mẫu trong tài liệu Supabase có đoạn chuyển hướng người chưa đăng nhập
 * về /login. Ở app này các trang là công khai có chủ đích, nên chép đoạn đó
 * vào sẽ chặn hết khách. Việc phân quyền nằm ở requireAdmin() trong từng
 * Server Action.
 */
export async function middleware(request: NextRequest) {
  let response = NextResponse.next({ request });

  const url = process.env.NEXT_PUBLIC_SUPABASE_URL;
  const key = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;
  // Thiếu cấu hình thì cho request đi tiếp như khách, không làm sập cả trang.
  if (!url || !key) return response;

  const supabase = createServerClient(url, key, {
    cookies: {
      getAll() {
        return request.cookies.getAll();
      },
      setAll(cookiesToSet) {
        cookiesToSet.forEach(({ name, value }) => request.cookies.set(name, value));
        response = NextResponse.next({ request });
        cookiesToSet.forEach(({ name, value, options }) =>
          response.cookies.set(name, value, options)
        );
      },
    },
  });

  // Không chèn bất kỳ code nào giữa createServerClient và getUser().
  await supabase.auth.getUser();

  return response;
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
};
```

- [ ] **Step 2: Tuyến callback**

`app/auth/callback/route.ts`:

```typescript
import { NextResponse, type NextRequest } from 'next/server';
import { taoServerClient } from '../../../lib/supabase/server';

/**
 * Google trả người dùng về Supabase, Supabase đẩy tiếp về đây kèm mã `code`.
 * Đổi mã lấy phiên rồi đưa họ về đúng trang đang xem trước khi đăng nhập.
 */
export async function GET(request: NextRequest) {
  const { searchParams, origin } = new URL(request.url);
  const code = searchParams.get('code');
  const next = searchParams.get('next') ?? '/';

  if (code) {
    const supabase = await taoServerClient();
    const { error } = await supabase.auth.exchangeCodeForSession(code);
    if (!error) {
      // Chỉ nhận đường dẫn nội bộ, chặn chuyển hướng ra ngoài.
      const dich = next.startsWith('/') && !next.startsWith('//') ? next : '/';
      return NextResponse.redirect(`${origin}${dich}`);
    }
    console.error('[đăng nhập] đổi mã lấy phiên thất bại:', error.message);
  }

  return NextResponse.redirect(`${origin}/?loi=dangnhap`);
}
```

- [ ] **Step 3: Action đăng nhập và đăng xuất**

`app/actions/auth.ts`:

```typescript
'use server';

import { headers } from 'next/headers';
import { redirect } from 'next/navigation';
import { revalidatePath } from 'next/cache';
import { taoServerClient } from '../../lib/supabase/server';

/**
 * Hai action trong tệp này KHÔNG gọi requireAdmin(): chưa đăng nhập thì làm
 * sao là admin được. Đây là ngoại lệ duy nhất, và test quét mã nguồn ở Task 5
 * có ghi nhận điều đó.
 */

export async function signInWithGoogle(next: string = '/') {
  const supabase = await taoServerClient();
  const origin = (await headers()).get('origin') ?? '';

  const { data, error } = await supabase.auth.signInWithOAuth({
    provider: 'google',
    options: {
      redirectTo: `${origin}/auth/callback?next=${encodeURIComponent(next)}`,
    },
  });

  if (error || !data.url) {
    console.error('[đăng nhập] không tạo được liên kết Google:', error?.message);
    throw new Error('Không mở được trang đăng nhập. Vui lòng thử lại.');
  }

  redirect(data.url);
}

export async function signOut() {
  const supabase = await taoServerClient();
  await supabase.auth.signOut();
  revalidatePath('/', 'layout');
}
```

- [ ] **Step 4: Kiểm tra biên dịch và build**

Run: `bun run lint && bun run build`
Expected: cả hai sạch.

- [ ] **Step 5: Commit**

```bash
git add middleware.ts app/auth/ app/actions/auth.ts
git commit -m "feat: Google sign-in flow

Middleware only refreshes the session. The Supabase sample redirects
anonymous visitors to a login page; copying that would lock out the
public read access this app is built around.

The callback only accepts internal redirect targets."
```

---

## Task 4: Nút đăng nhập trên giao diện

**Files:**
- Create: `components/AuthButton.tsx`, `components/LoginModal.tsx`
- Modify: `components/Navbar.tsx`, `app/[monthKey]/layout.tsx`

**Interfaces:**
- Consumes: `signInWithGoogle`, `signOut` từ Task 3; `getSessionUser`, `getVaiTro` từ Task 2.
- Produces: `<AuthButton email={...} isAdmin={...} />`; Navbar nhận thêm `isSuperAdmin`.

- [ ] **Step 1: Modal đăng nhập**

`components/LoginModal.tsx`:

```tsx
'use client';

import React, { useTransition } from 'react';
import { X } from 'lucide-react';
import { signInWithGoogle } from '../app/actions/auth';

export function LoginModal({ next, onClose }: { next: string; onClose: () => void }) {
  const [dangChay, startTransition] = useTransition();

  return (
    <div
      className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/40 p-4"
      onClick={onClose}
    >
      <div
        className="w-full max-w-sm rounded-2xl border border-slate-200 bg-white p-6 shadow-xl"
        onClick={(e) => e.stopPropagation()}
      >
        <div className="flex items-start justify-between">
          <div>
            <h2 className="text-base font-bold text-slate-900">Đăng nhập</h2>
            <p className="mt-1 text-xs text-slate-500">
              Chỉ chủ nhóm mới cần đăng nhập để ghi dữ liệu. Xem thì không cần.
            </p>
          </div>
          <button
            type="button"
            onClick={onClose}
            className="rounded-lg p-1 text-slate-400 hover:bg-slate-100 hover:text-slate-600"
            aria-label="Đóng"
          >
            <X className="h-4 w-4" />
          </button>
        </div>

        <button
          type="button"
          disabled={dangChay}
          onClick={() => startTransition(() => void signInWithGoogle(next))}
          className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl border border-slate-300 bg-white px-4 py-3 text-sm font-bold text-slate-800 transition-colors hover:bg-slate-50 disabled:opacity-60 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"
        >
          {dangChay ? 'Đang chuyển tới Google...' : 'Tiếp tục với Google'}
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Nút trạng thái đăng nhập**

`components/AuthButton.tsx`:

```tsx
'use client';

import React, { useState, useTransition } from 'react';
import { usePathname } from 'next/navigation';
import { LogIn, LogOut, ShieldCheck } from 'lucide-react';
import { signOut } from '../app/actions/auth';
import { LoginModal } from './LoginModal';

export function AuthButton({ email, isAdmin }: { email: string | null; isAdmin: boolean }) {
  const [moModal, setMoModal] = useState(false);
  const [dangChay, startTransition] = useTransition();
  const pathname = usePathname();

  if (!email) {
    return (
      <>
        <button
          type="button"
          onClick={() => setMoModal(true)}
          className="inline-flex items-center gap-1.5 rounded-xl border border-slate-200 bg-white px-2.5 py-1.5 text-xs font-semibold text-slate-600 transition-colors hover:text-indigo-600"
        >
          <LogIn className="h-3.5 w-3.5" />
          <span className="hidden sm:inline">Đăng nhập</span>
        </button>
        {moModal && <LoginModal next={pathname} onClose={() => setMoModal(false)} />}
      </>
    );
  }

  return (
    <div className="flex items-center gap-1.5">
      {isAdmin && (
        <span
          className="inline-flex items-center gap-1 rounded-lg bg-emerald-50 px-2 py-1 text-[11px] font-bold text-emerald-700"
          title={email}
        >
          <ShieldCheck className="h-3 w-3" />
          <span className="hidden sm:inline">Chủ nhóm</span>
        </span>
      )}
      <button
        type="button"
        disabled={dangChay}
        onClick={() => startTransition(() => void signOut())}
        title={email}
        className="inline-flex items-center gap-1.5 rounded-xl border border-slate-200 bg-white px-2.5 py-1.5 text-xs font-semibold text-slate-600 transition-colors hover:text-indigo-600 disabled:opacity-60"
      >
        <LogOut className="h-3.5 w-3.5" />
        <span className="hidden sm:inline">Thoát</span>
      </button>
    </div>
  );
}
```

- [ ] **Step 3: Nối vào Navbar**

Trong `components/Navbar.tsx`, thêm hai prop vào `NavbarProps`:

```typescript
  /** Email người đang đăng nhập, null nếu là khách. */
  email: string | null;
  /** Đúng với cả admin lẫn super admin — điều khiển mọi nút nghiệp vụ. */
  isAdmin: boolean;
  /** Chỉ điều khiển đúng một thứ: mục quản lý admin trong menu tiện ích. */
  isSuperAdmin: boolean;
```

Destructure `email, isAdmin, isSuperAdmin` trong tham số component, import `AuthButton`, và đặt nó cạnh nút menu tiện ích ở hàng trên cùng:

```tsx
<AuthButton email={email} isAdmin={isAdmin} />
```

- [ ] **Step 4: Truyền dữ liệu từ layout**

Trong `app/[monthKey]/layout.tsx`, thêm import và lấy trạng thái:

```typescript
import { getSessionUser, getVaiTro } from '../../lib/auth/session';
```

Trong thân hàm, sau khi đã có `data`:

```typescript
  const [user, vaiTro] = await Promise.all([getSessionUser(), getVaiTro()]);
  const isAdmin = vaiTro === 'admin' || vaiTro === 'super_admin';
  const isSuperAdmin = vaiTro === 'super_admin';
```

Rồi truyền xuống:

```tsx
<Navbar
  ...
  email={user?.email ?? null}
  isAdmin={isAdmin}
  isSuperAdmin={isSuperAdmin}
/>
```

- [ ] **Step 5: Kiểm tra biên dịch và build**

Run: `bun run lint && bun run build`
Expected: cả hai sạch.

- [ ] **Step 6: Commit**

```bash
git add components/AuthButton.tsx components/LoginModal.tsx components/Navbar.tsx "app/[monthKey]/layout.tsx"
git commit -m "feat: sign-in button and modal in the navbar"
```

---

## Task 5: Chốt chặn trên mọi Server Action

Task quan trọng nhất. Test quét mã nguồn ở đây là thứ ngăn một lỗ hổng im lặng về sau.

**Files:**
- Modify: `app/actions/daily-sessions.ts`, `app/actions/members.ts`, `app/actions/months.ts`, `app/actions/settlement.ts`
- Create: `app/actions/actions-guard.test.ts`

**Interfaces:**
- Consumes: `requireAdmin` từ Task 2.

- [ ] **Step 1: Viết test quét mã nguồn — nó phải đỏ trước**

`app/actions/actions-guard.test.ts`:

```typescript
import { readFileSync, readdirSync } from 'node:fs';
import { join } from 'node:path';
import { describe, expect, it } from 'vitest';

const THU_MUC = join(process.cwd(), 'app/actions');

/**
 * auth.ts được miễn: hai action trong đó phục vụ chính việc đăng nhập, chưa
 * đăng nhập thì không thể là admin.
 */
const MIEN_TRU = new Set(['auth.ts']);

/** Các tệp mà mọi action bên trong BẮT BUỘC phải là requireSuperAdmin(). */
const CHI_SUPER_ADMIN = new Set(['admins.ts']);

function layThanHam(nguon: string, tenHam: string): string {
  const bd = nguon.indexOf(`export async function ${tenHam}`);
  const moNgoac = nguon.indexOf('{', bd);
  let sau = 0;
  for (let i = moNgoac; i < nguon.length; i += 1) {
    if (nguon[i] === '{') sau += 1;
    if (nguon[i] === '}') {
      sau -= 1;
      if (sau === 0) return nguon.slice(moNgoac, i + 1);
    }
  }
  return nguon.slice(moNgoac);
}

describe('mọi Server Action ghi dữ liệu đều có chốt chặn', () => {
  const tepCanKiem = readdirSync(THU_MUC)
    .filter((f) => f.endsWith('.ts') && !f.endsWith('.test.ts'))
    .filter((f) => !MIEN_TRU.has(f));

  it('tìm thấy các tệp action để kiểm', () => {
    expect(tepCanKiem.length).toBeGreaterThan(0);
  });

  for (const tep of tepCanKiem) {
    const nguon = readFileSync(join(THU_MUC, tep), 'utf8');
    const tenHam = [...nguon.matchAll(/export async function (\w+)/g)].map((m) => m[1]);

    it(`${tep} có ít nhất một action`, () => {
      expect(tenHam.length).toBeGreaterThan(0);
    });

    const chiSuperAdmin = CHI_SUPER_ADMIN.has(tep);

    for (const ten of tenHam) {
      it(`${tep} → ${ten}() có chốt chặn`, () => {
        const than = layThanHam(nguon, ten);
        expect(
          than.includes('requireAdmin()') || than.includes('requireSuperAdmin()')
        ).toBe(true);
      });

      if (chiSuperAdmin) {
        // Kiểm riêng, vì phép kiểm "có chốt chặn nào đó" ở trên vẫn xanh khi ai
        // đó hạ requireSuperAdmin() xuống requireAdmin() — mà đó chính là lỗi
        // xóa mất ranh giới giữa hai nhóm quyền.
        it(`${tep} → ${ten}() dùng đúng requireSuperAdmin()`, () => {
          const than = layThanHam(nguon, ten);
          expect(than).toContain('requireSuperAdmin()');
        });
      }
    }
  }
});
```

- [ ] **Step 2: Chạy test để xác nhận nó thất bại**

Run: `bun run test app/actions/actions-guard.test.ts`
Expected: FAIL — 10 test báo thiếu chốt chặn. Nếu nó PASS ngay thì test hỏng, phải sửa test trước khi đi tiếp.

(`admins.ts` chưa tồn tại ở bước này; Task 7 tạo nó và các test super admin sẽ tự xuất hiện.)

- [ ] **Step 3: Thêm chốt chặn vào từng tệp**

Trong **cả bốn** tệp, thêm import:

```typescript
import { requireAdmin } from '../../lib/auth/session';
```

Rồi đặt `await requireAdmin();` làm **câu lệnh đầu tiên** trong thân của cả 10 hàm:

`daily-sessions.ts` — `saveDailySession`, `deleteDailySession`
`members.ts` — `createMember`, `updateMember`, `removeMemberFromMonth`, `addExistingMemberToMonth`
`months.ts` — `createMonth`, `updateMonth`, `deleteMonth`
`settlement.ts` — `toggleTransferSettled`

Ví dụ:

```typescript
export async function deleteDailySession(monthKey: string, sessionId: string) {
  await requireAdmin();

  await db.delete(dailySessions).where(eq(dailySessions.id, sessionId));
  revalidatePath(`/${monthKey}`, 'layout');
}
```

Đặt đầu tiên là có chủ đích: không truy vấn, không tính toán, không ghi log gì trước khi biết người gọi có quyền hay không.

- [ ] **Step 4: Chạy test để xác nhận nó qua**

Run: `bun run test`
Expected: PASS toàn bộ, trong đó 10 test chốt chặn.

- [ ] **Step 5: Kiểm chứng test thật sự bắt được lỗi**

Xóa tạm dòng `await requireAdmin();` khỏi `toggleTransferSettled`, chạy lại test.
Expected: FAIL đúng một test — `settlement.ts → toggleTransferSettled() có chốt chặn`.
Khôi phục lại dòng vừa xóa, chạy lại, PASS.

Bước này không được bỏ qua: một test luôn xanh thì vô dụng, và cách duy nhất để biết là làm nó đỏ.

- [ ] **Step 6: Commit**

```bash
git add app/actions/
git commit -m "feat: require admin for every write action

Adds a source-scanning test that fails if any Server Action is missing
its requireAdmin() call. A forgotten guard is a silent hole — nothing
errors, nothing warns, there is just an unprotected write path — so the
test enumerates the actions rather than trusting review."
```

---

## Task 6: Ẩn nút ghi với người không đủ quyền

**Files:**
- Modify: `app/[monthKey]/page.tsx`, `app/[monthKey]/settlement/page.tsx`, `app/[monthKey]/members/page.tsx`, `components/DailySessionsTab.tsx`, `components/DailySessionList.tsx`, `components/CalendarView.tsx`, `components/SettlementView.tsx`, `components/MySettlement.tsx`, `components/MemberView.tsx`, `components/Navbar.tsx`

- [ ] **Step 1: Ba trang lấy trạng thái và truyền xuống**

Trong mỗi trang, thêm:

```typescript
import { getVaiTro } from '../../lib/auth/session';
```

(đường dẫn tương đối sâu hơn một cấp với hai trang trong thư mục con), rồi:

```typescript
  const vaiTro = await getVaiTro();
  const isAdmin = vaiTro === 'admin' || vaiTro === 'super_admin';
```

và truyền `isAdmin={isAdmin}` vào component gốc của trang.

`app/[monthKey]/layout.tsx` cần thêm `isSuperAdmin={vaiTro === 'super_admin'}` cho `Navbar`, vì chỉ super admin mới thấy mục quản lý admin.

- [ ] **Step 2: Luồn prop xuống và ẩn nút**

Thêm `isAdmin: boolean` vào props của từng component trong danh sách trên, rồi bọc mọi nút ghi bằng `{isAdmin && (...)}`.

Danh sách nút phải ẩn:

| Component | Nút |
|---|---|
| `DailySessionList` | "Ghi Buổi Đánh Mới", sửa, xóa, nhân bản |
| `CalendarView` | nút "+" trên ô ngày, onClick thêm buổi của ô ngày, sửa, xóa |
| `SettlementView` | nút đánh dấu đã chuyển khoản ở từng dòng |
| `MySettlement` | nút "Tôi đã chuyển" và "Đã nhận" |
| `MemberView` | thêm, sửa, gỡ thành viên, tải QR |
| `Navbar` | mục "Sửa / Xóa kỳ đánh" trong menu, nút tạo kỳ trong bộ chọn tháng |

Với `CalendarView`, khi không phải admin thì bỏ luôn `onClick` và `role="button"` trên ô ngày — để lại thì người dùng bấm mà không có gì xảy ra, tệ hơn là không bấm được.

- [ ] **Step 3: Kiểm tra biên dịch và build**

Run: `bun run lint && bun run build`
Expected: cả hai sạch.

- [ ] **Step 4: Commit**

```bash
git add app components
git commit -m "feat: hide write controls from non-admins

Hidden rather than disabled: a button that cannot do anything is worse
than no button. This is only a courtesy to the reader — the real block
is requireAdmin() on the server."
```

---

## Task 7: Quản lý danh sách admin

**Files:**
- Create: `app/actions/admins.ts`, `components/AdminsModal.tsx`
- Modify: `components/Navbar.tsx`

**Interfaces:**
- Consumes: `requireSuperAdmin`, `getSessionUser` từ Task 2; `listAdmins` từ Task 2.
- Produces: `addAdmin(email: string)`, `removeAdmin(email: string)`.

- [ ] **Step 1: Server Action**

`app/actions/admins.ts`:

```typescript
'use server';

import { eq } from 'drizzle-orm';
import { revalidatePath } from 'next/cache';
import { db } from '../../db';
import { admins } from '../../db/schema';
import { getSessionUser, requireSuperAdmin } from '../../lib/auth/session';
import { laEmailAdmin } from '../../lib/auth/admin-emails';

/** Đủ để loại các chuỗi rõ ràng không phải email; không cố bắt mọi trường hợp. */
const DANG_EMAIL = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

export async function addAdmin(email: string) {
  await requireSuperAdmin();

  const chuanHoa = email.trim().toLowerCase();
  if (!DANG_EMAIL.test(chuanHoa)) {
    throw new Error('Địa chỉ email không hợp lệ.');
  }

  // Người đã là super admin thì thêm vào bảng cũng vô nghĩa: getVaiTro() luôn
  // trả super_admin trước khi tra bảng. Báo rõ thay vì ghi một dòng vô dụng.
  if (laEmailAdmin(chuanHoa)) {
    throw new Error('Email này đã là super admin từ cấu hình hệ thống.');
  }

  const nguoiThem = await getSessionUser();

  await db
    .insert(admins)
    .values({ email: chuanHoa, addedBy: nguoiThem?.email ?? null })
    .onConflictDoNothing();

  revalidatePath('/', 'layout');
}

export async function removeAdmin(email: string) {
  await requireSuperAdmin();

  await db.delete(admins).where(eq(admins.email, email.trim().toLowerCase()));
  revalidatePath('/', 'layout');
}
```

- [ ] **Step 2: Chạy test chốt chặn**

Run: `bun run test app/actions/actions-guard.test.ts`
Expected: PASS, và xuất hiện thêm bốn test mới cho `admins.ts` — hai test "có chốt chặn" và hai test "dùng đúng requireSuperAdmin()".

- [ ] **Step 3: Kiểm chứng test bắt được việc hạ quyền**

Đổi tạm `requireSuperAdmin()` thành `requireAdmin()` trong `addAdmin` (nhớ sửa cả import), chạy lại test.
Expected: FAIL đúng một test — `admins.ts → addAdmin() dùng đúng requireSuperAdmin()`. Test "có chốt chặn" vẫn xanh, đúng như thiết kế — đó là lý do phải có phép kiểm thứ hai.

Khôi phục lại, chạy lại, PASS.

- [ ] **Step 4: Modal quản lý**

`components/AdminsModal.tsx`:

```tsx
'use client';

import React, { useState, useTransition } from 'react';
import { ShieldCheck, Trash2, X } from 'lucide-react';
import { addAdmin, removeAdmin } from '../app/actions/admins';

export interface DongAdmin {
  email: string;
  addedAt: string;
  addedBy: string | null;
  laSuperAdmin: boolean;
}

export function AdminsModal({
  danhSach,
  onClose,
}: {
  danhSach: DongAdmin[];
  onClose: () => void;
}) {
  const [emailMoi, setEmailMoi] = useState('');
  const [loi, setLoi] = useState<string | null>(null);
  const [dangChay, startTransition] = useTransition();

  const them = () => {
    setLoi(null);
    startTransition(async () => {
      try {
        await addAdmin(emailMoi);
        setEmailMoi('');
      } catch (e) {
        setLoi(e instanceof Error ? e.message : 'Không thêm được.');
      }
    });
  };

  return (
    <div
      className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/40 p-4"
      onClick={onClose}
    >
      <div
        className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-5 shadow-xl"
        onClick={(e) => e.stopPropagation()}
      >
        <div className="flex items-start justify-between">
          <div>
            <h2 className="text-base font-bold text-slate-900">Quản lý quyền</h2>
            <p className="mt-1 text-xs text-slate-500">
              Người trong danh sách này ghi được dữ liệu. Thêm xong nhớ nhắn họ đăng nhập.
            </p>
          </div>
          <button
            type="button"
            onClick={onClose}
            className="rounded-lg p-1 text-slate-400 hover:bg-slate-100"
            aria-label="Đóng"
          >
            <X className="h-4 w-4" />
          </button>
        </div>

        <ul className="mt-4 space-y-1.5">
          {danhSach.map((d) => (
            <li
              key={d.email}
              className="flex items-center justify-between gap-2 rounded-xl border border-slate-200 px-3 py-2"
            >
              <span className="min-w-0">
                <span className="block truncate text-sm font-semibold text-slate-800">
                  {d.email}
                </span>
                <span className="text-[11px] text-slate-400">
                  {d.laSuperAdmin ? 'Từ cấu hình hệ thống' : `Thêm ngày ${d.addedAt}`}
                </span>
              </span>

              {d.laSuperAdmin ? (
                <ShieldCheck className="h-4 w-4 shrink-0 text-emerald-600" />
              ) : (
                <button
                  type="button"
                  disabled={dangChay}
                  onClick={() => startTransition(() => void removeAdmin(d.email))}
                  className="shrink-0 rounded-lg p-1.5 text-slate-400 hover:bg-rose-50 hover:text-rose-600 disabled:opacity-60"
                  aria-label={`Gỡ quyền của ${d.email}`}
                >
                  <Trash2 className="h-4 w-4" />
                </button>
              )}
            </li>
          ))}
        </ul>

        <div className="mt-4 flex gap-2">
          <input
            type="email"
            value={emailMoi}
            onChange={(e) => setEmailMoi(e.target.value)}
            placeholder="email@gmail.com"
            className="min-w-0 flex-1 rounded-xl border border-slate-200 px-3 py-2 text-sm focus:border-indigo-400 focus:outline-hidden"
          />
          <button
            type="button"
            disabled={dangChay || !emailMoi.trim()}
            onClick={them}
            className="shrink-0 rounded-xl bg-indigo-600 px-3.5 py-2 text-sm font-bold text-white hover:bg-indigo-700 disabled:opacity-60"
          >
            Thêm
          </button>
        </div>

        {loi && <p className="mt-2 text-xs font-semibold text-rose-600">{loi}</p>}
      </div>
    </div>
  );
}
```

- [ ] **Step 5: Nối vào Navbar**

`app/[monthKey]/layout.tsx` gọi `listAdmins()` khi `vaiTro === 'super_admin'` (khách thì không cần truy vấn), dựng mảng `DongAdmin` — gộp email từ `ADMIN_EMAILS` với `laSuperAdmin: true` lên đầu, rồi tới các dòng trong bảng — và truyền xuống `Navbar`.

Trong `Navbar`, thêm mục "Quản lý quyền" vào menu tiện ích, bọc trong `{isSuperAdmin && ...}`, mở `AdminsModal`.

`addedAt` phải định dạng ở server thành chuỗi trước khi truyền xuống client, tránh lệch múi giờ giữa server và trình duyệt.

- [ ] **Step 6: Kiểm tra biên dịch và build**

Run: `bun run lint && bun run build`
Expected: cả hai sạch.

- [ ] **Step 7: Commit**

```bash
git add app/actions/admins.ts components/AdminsModal.tsx components/Navbar.tsx "app/[monthKey]/layout.tsx"
git commit -m "feat: super admin can manage the admin list

Admins live in a table; super admins come from ADMIN_EMAILS and cannot
be removed through the UI. The guard test now also asserts that the two
management actions use requireSuperAdmin() specifically — checking only
that *some* guard exists would stay green if someone downgraded them."
```

---

## Task 8: Kiểm chứng thủ công và tài liệu

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Chuẩn bị**

Điền `.env` đủ 3 biến mới: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `ADMIN_EMAILS`.
Xác nhận đã bật Google provider trong Supabase và khai báo redirect URL.

```bash
bun run build && bun run start
```

- [ ] **Step 2: Chạy qua 9 tiêu chí nghiệm thu của spec**

1. Chưa đăng nhập: xem được cả ba tab, quét được QR.
2. Chưa đăng nhập: không thấy nút ghi nào.
3. Đăng nhập bằng email không thuộc nhóm nào: vẫn không thấy nút ghi.
4. Đăng nhập bằng email **trong** `ADMIN_EMAILS`: mọi nút hiện ra, và menu tiện ích có mục "Quản lý quyền".
5. Super admin thêm một email vào danh sách; người đó đăng nhập và ghi được dữ liệu, nhưng **không thấy** mục "Quản lý quyền".
6. Gỡ người đó khỏi danh sách; họ tải lại trang và mất hết nút ghi.
7. Super admin trong `ADMIN_EMAILS` hiện trong danh sách nhưng **không có nút gỡ**.
8. Đăng xuất rồi tải lại: quay về trạng thái khách.
9. Đặt `ADMIN_EMAILS=` rỗng rồi khởi động lại: không còn super admin, mục quản lý quyền biến mất. Admin trong bảng vẫn ghi được dữ liệu.

- [ ] **Step 3: Kiểm chứng chốt chặn phía server không phụ thuộc giao diện**

Đây là phép thử quan trọng nhất — ẩn nút không phải là bảo mật.

Mở DevTools → Network, đăng nhập bằng tài khoản **không** phải admin, rồi thử gọi lại một Server Action bằng cách phát lại request (Copy as fetch rồi chạy trong Console).
Expected: bị từ chối, và dữ liệu trong database **không đổi**. Kiểm lại bằng Supabase MCP hoặc `drizzle-kit studio`.

Làm lại phép thử đó với tài khoản **admin thường** gọi `addAdmin`.
Expected: bị từ chối, bảng `admins` không đổi. Đây là phép thử chứng minh ranh giới giữa admin và super admin có thật, không chỉ là ẩn nút.

- [ ] **Step 4: Cập nhật README**

Thêm mục "Đăng nhập" ghi rõ: ba biến môi trường mới, cách lấy Client ID và Secret từ Google Cloud Console, redirect URI phải là `https://<project-ref>.supabase.co/auth/v1/callback`, và ghi chú rằng để app ở trạng thái Testing của Google có tác dụng như một danh sách cho phép.

Ghi cả sự khác nhau giữa hai mức quyền: super admin sửa bằng biến môi trường và phải deploy lại, admin thêm bớt ngay trong app.

- [ ] **Step 5: Commit**

```bash
git add README.md
git commit -m "docs: document sign-in setup and admin configuration"
```

---

## Tự rà soát

**Bao phủ spec:** ba mức quyền khách/admin/super admin (Task 2, 5, 6, 7), `ADMIN_EMAILS` với so khớp bỏ qua hoa thường và khoảng trắng (Task 1), hỏng theo hướng an toàn khi biến rỗng (Task 1), bảng `admins` (Task 2), biến môi trường thắng khi trùng (Task 2), Google OAuth (Task 3), phiên trong cookie qua `@supabase/ssr` (Task 2), middleware chỉ làm mới không chặn (Task 3), `requireAdmin()` ở cả 10 action nghiệp vụ (Task 5), `requireSuperAdmin()` ở hai action quản lý (Task 7), test quét mã nguồn kiểm cả hai loại (Task 5, 7), ẩn nút theo quyền (Task 6), màn hình quản lý admin (Task 7), không gỡ được super admin (Task 7), không có luồng email mời (không làm — đúng như spec), QR vẫn công khai (không đụng tới — đúng như spec), chín tiêu chí nghiệm thu (Task 8).

**Điểm cần lưu ý khi thực hiện:**

- Task 5 Step 5 và Task 7 Step 3 bắt buộc phải làm. Một test chốt chặn chưa từng đỏ thì không ai biết nó có hoạt động không — và riêng Task 7 Step 3 chứng minh phép kiểm thứ hai là cần thiết, vì phép kiểm thứ nhất vẫn xanh khi quyền bị hạ.
- Task 6 chạm nhiều tệp nhất. Nếu thấy nặng, tách theo từng tab và commit riêng.
- Task 2 chạy migration lên database thật. Bảng `admins` rỗng nên không có rủi ro mất dữ liệu, nhưng vẫn phải chạy `db:generate` trước `db:migrate`.
- Đường dẫn import tương đối khác nhau giữa `app/[monthKey]/page.tsx` và `app/[monthKey]/settlement/page.tsx` — sâu hơn một cấp.
- `signInWithGoogle` gọi `redirect()`, mà `redirect()` hoạt động bằng cách ném lỗi. Không bọc nó trong `try/catch` nuốt lỗi, nếu không luồng đăng nhập sẽ đứng im không báo gì.
